# Personal folder (local only)

Create `personal/` for your annotations, interview prep drafts, and bookmarks.
Already in `.gitignore` — never pushed to your fork or upstream.

```bash
mkdir -p personal
```
